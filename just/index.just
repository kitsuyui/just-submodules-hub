# Shared command modules.
#
# Command naming rules:
# - Canonical recipes should form a namespace/resource tree with the verb at the leaf.
# - See docs/command-naming.md before adding or renaming recipes.

mod repo "repo/index.just"
mod github "github/index.just"
